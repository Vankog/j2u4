# Secrets and session data
config.json
session.json
mapping.json
account_to_arbauft_mapping.json
ui_inspection.json

# Failure captures (Playwright traces, videos, context.json — may contain DOM contents)
captures/

# Per-run sync history (worklog ids, ticket keys, ArbAuft codes — may contain customer info)
sync_history.log

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
build/
dist/
*.egg-info/

# IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
